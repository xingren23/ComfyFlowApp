{
  "title": "ImageGen(SD 1.5)",
  "description": "Generate images from prompt with SD 1.5",
  "inputs": {
    
    "6": {
      "inputs": {
        "text": {
          "type": "STRING",
          "name": "Prompt(Positive)",
          "help": "the positive prompt to generate images",
          "default": "1gril beauty walking in the steet, blur background",
          "max": 300
        }
      }
    },
    "7": {
      "inputs": {
        "text": {
          "type": "STRING",
          "name": "Prompt(Negative)",
          "help": "the negative prompt to generate images",
          "default": "((nsfw)),sketches,nude, (worse quality:2),(low quality:2),(normal quality:2) lowers normal quality, ((monochrome)),((grayscale)), facing away,looking away, text,error,extra digit,signature,watermark,username,blurry, skin spots,acnes,skin blemishes,bad anatomy,fat,bad feet,cropped,poorly drawn hands,poorly drawn face,mutation,deformed,",
          "max": 300
        }
      }
    },
    "3": {
      "inputs": {
        "seed": {
          "type": "INT",
          "name": "Seed",
          "help": "the seed to generate images",
          "default": 0,
          "min": 0,
          "max": 4503599627370496,
          "step": 1
        }
      }
    }
  },
  "outputs": {
    "9": {
      "outputs": {
        "filename_prefix": "ComfyFlowApp"
      }
    }
  }
}